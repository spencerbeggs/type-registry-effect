---
"type-registry-effect": patch
---

## Other

Migrate Effect ecosystem dependencies to `catalog:silk` and `catalog:silkPeers` for centralized version management via `@savvy-web/pnpm-plugin-silk`. Fixes #12.
